if [[ -z "$CONFIG" ]]; then
    python -m {% include 'package_folder_slug.j2' %}.app --galaxy-history-id $HISTORY_ID --host 0.0.0.0 --server timeout=0
else
    python -m {% include 'package_folder_slug.j2' %}.app --config $CONFIG --galaxy-history-id $HISTORY_ID --host 0.0.0.0 --server timeout=0
fi
